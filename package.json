{
  "type": "module",
  "scripts": {
    "compile": "tsc",
    "start": "npm run compile && node ./build/src/index.js",
    "go": "node src/index.ts",
    "lint": "gts lint",
    "clean": "gts clean",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "dependencies": {
    "@apollo/datasource-rest": "^4.3.2",
    "@apollo/server": "^4.1.1",
    "graphql": "^16.6.0",
    "graphql-tag": "^2.12.6"
  },
  "devDependencies": {
    "@types/node": "^14.11.2",
    "gts": "^3.1.1",
    "ts-node": "^10.9.1",
    "typescript": "~4.7.0"
  }
}
